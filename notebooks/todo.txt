a) implement HOPE to compare chunking strategies
b) check for hallucinations in generated title names.
I could do this by taking a random sample of jsons, exporting the alleged title and original pdf file name to excel, while copying the sampled pdf in a folder so I can read them easily
c) implement hybrid search - i imagine i might need to use a better model for generating summaries first should gemma3.1:1b be found insufficient. but how can it? it seems subjective when the answer is factual
d) implement an agentic pipeline
e) try yet different embeddings, like a different embedding model than i used (probably m3. multilingual to see if it is an improvement)
f) reranking... reranking of what I currently retrieve? what if what i retrieve is crap anyway?
g) LateSplit
h) something about chain of rank, chain of note... i never looked into it really.
i) add arxiv/doi link to metadata. seems nice, but also not strictly necessary? I could probably defend a thesis without thesis
What my advisor tariq said last meeting:
##
verify if it hallucinates title names, but later. for now lets have a complete pipeline.
 then we can try to improve. my contribution - enhance above baseline. he says lets make a very good version of this and THEN use a knowledge graph.
  handle hallucinations, reranking. 
  later graph see if it enhances or not.
## 