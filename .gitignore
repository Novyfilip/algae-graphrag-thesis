# Environment and secrets
thesis_env/
.env

# Python artifacts
__pycache__/
*.pyc
.ipynb_checkpoints/

# Data — keeps only community summaries (small, human-readable, expensive to regenerate)
ingestion/data/kg_extractions/
data/chromadb/
data/chroma_db/
data/chromadb_deploy/
data/kg_extractions/
data/extracted/
data/raw/
data/test/
data/chunks/
data/processed/
!data/community_summaries/

# Development scratch, backups, and process artifacts
scratch/
backup/
context/

# Quarto generated files
log_files/
thesis_files/
*.html
*.tex

# Windows shortcuts
*.lnk

# PDFs (except the final thesis if you want to include it)
*.pdf

# Outputs — keep only the final evaluation results
outputs/
!outputs/ragas_final/
!outputs/ragas_extended/

# Quarto source files (optional — remove these lines if you want the log in the repo)
*.qmd
